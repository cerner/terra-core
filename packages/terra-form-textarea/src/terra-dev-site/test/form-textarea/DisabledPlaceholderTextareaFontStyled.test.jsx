import React from 'react';

import Textarea from '../../../Textarea';

const styles = {
  fontStyle: 'italic',
};

const textarea = () => <Textarea disabled placeholder="placeholder" style={styles} />;

export default textarea;
