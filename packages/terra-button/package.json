{
  "name": "terra-button",
  "main": "lib/Button.js",
  "private": true,
  "version": "0.2.0",
  "description": "The terra-button component provides users a way to trigger actions in the UI.",
  "repository": {
    "type": "git",
    "url": "git+https://github.cerner.com/terra/terra-ui.git"
  },
  "keywords": [
    "Button",
    "Cerner",
    "Terra",
    "terra-button",
    "UI"
  ],
  "author": "Cerner Corporation",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.cerner.com/terra/terra-ui/issues"
  },
  "homepage": "https://github.cerner.com/terra/terra-ui#readme",
  "scripts": {
    "compile": "../../node_modules/.bin/babel src --out-dir lib --copy-files",
    "lint": "npm run lint:js && npm run lint:scss",
    "lint:js": "../../node_modules/.bin/eslint **/*.js, **/*.jsx",
    "lint:scss": "../../node_modules/.bin/stylelint src/**/*.scss",
    "pretest": "npm run lint",
    "release:major": "npm test && npm run compile && npm version major -m \"Released version %s\" && npm publish && git push",
    "release:minor": "npm test && npm run compile && npm version minor -m \"Released version %s\" && npm publish && git push",
    "release:patch": "npm test && npm run compile && npm version patch -m \"Released version %s\" && npm publish && git push",
    "test": "../../node_modules/.bin/jest --config ../../jestconfig.json"
  },
  "peerDependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "terra-mixins": "^1.0.0"
  }
}
