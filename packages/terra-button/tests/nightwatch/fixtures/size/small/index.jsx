import React from 'react';
import ReactDOM from 'react-dom';

import Button from '../../../../../lib/Button';

const button = <Button text="Small" size="small" />;

ReactDOM.render(button, document.getElementById('terra-button'));
