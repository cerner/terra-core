import React from 'react';

import Heading from '../../lib/Heading';

const HeadingWeight = () => (
  <div>
    <Heading level={1} weight={700}>700 Weight</Heading>
    <Heading level={1} weight={400}>400 Weight</Heading>
    <Heading level={1} weight={200}>200 Weight</Heading>
  </div>
);

export default HeadingWeight;
