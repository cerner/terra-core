/* eslint-disable import/no-extraneous-dependencies */
import React from 'react';
import { Link } from 'react-router';

const PaginatorTests = () => (
  <div>
    <ul>
      <li><Link to="/tests/paginator-tests/default">Paginator - Default</Link></li>
    </ul>
  </div>
);

export default PaginatorTests;
