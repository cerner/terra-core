language: node_js
sudo: required
jdk: oraclejdk8
install: true
script:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
  - npm run test
cache:
  yarn: true
  directories:
    - node_modules
branches:
  only:
    - master
    