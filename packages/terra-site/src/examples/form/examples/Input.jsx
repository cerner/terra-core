import React from 'react';
import { Input } from 'terra-form';

const InputExamples = () => (
  <form>
    <Input
      name="foo"
      defaultValue="bar"
    />
  </form>
);

export default InputExamples;
