import React from 'react';
import Input from 'terra-form/lib/Input';

const InputExamples = () => (
  <form>
    <Input
      name="foo"
      defaultValue="bar"
    />
  </form>
);

export default InputExamples;
