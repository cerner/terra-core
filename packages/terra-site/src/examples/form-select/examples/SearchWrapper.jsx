import React from 'react';
import SearchExample from './Search';

const SearchWrapper = () => (
  <div>
    <p>A search variant provides a searchable single select.</p>
    <div style={{ paddingBottom: '15px', width: '300px' }}>
      <SearchExample />
    </div>
  </div>
);

export default SearchWrapper;
