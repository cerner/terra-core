/**
 * The Object representation of the row selection column.
 */
const ROW_SELECTION_COLUMN = {
  id: 'table-rowSelectionColumn',
  width: 40,
  isSelectable: true,
  isResizable: false,
};

const rowSelectionUtils = {
  ROW_SELECTION_COLUMN,
};

export default rowSelectionUtils;
