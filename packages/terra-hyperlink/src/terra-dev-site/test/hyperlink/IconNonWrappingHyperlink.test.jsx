import React from 'react';
import A from '../../../Hyperlink';

export default () => <div>Example of a hyperlink inside some content that will wrap. With this, the icon should not wrap onto a separate line without the link text. I am a <A href="https://www.cerner.com" variant="document">Document hyperlink</A> that is forced to wrap due to space constraints. My icon wraps with me.</div>;
